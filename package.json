{
  "name": "claude-mobile-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "A local, mobile-friendly bridge for Claude CLI",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
